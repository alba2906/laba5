<Window x:Class="Laba1.Windows.Reference"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Laba1.Windows"
        mc:Ignorable="d"
        Title="Справка"
        Width="800"
        Height="550"
        WindowStartupLocation="CenterScreen"
        MinWidth="800"
        MaxWidth="800">

    <ScrollViewer 
        VerticalScrollBarVisibility="Auto" 
        HorizontalScrollBarVisibility="Disabled"
        Padding="20">

        <StackPanel>

            <!-- Заголовок -->
            <TextBlock 
                FontSize="24" 
                FontWeight="Bold" 
                TextAlignment="Center" 
                Margin="0,0,0,20"
                TextWrapping="Wrap">
                Справка по программе
            </TextBlock>

            <!-- Меню Файл -->
			<Border Background="LightGray" Padding="10" Margin="0,5,0,10" CornerRadius="5">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="Bold" Margin="0,0,0,10">
                        Меню "Файл"
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Создать</Bold> - создает новый файл. При наличии несохраненных изменений программа предложит сохранить текущий файл.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Открыть</Bold> - открывает существующий файл. Поддерживаются форматы Poo и TXT.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Сохранить</Bold> - сохраняет текущий файл. Если файл ранее не был сохранен, открывается диалог "Сохранить как".
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Сохранить как</Bold> - сохраняет файл с указанием нового имени и расположения. Поддерживаются форматы Poo и TXT.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Выход</Bold> - закрывает программу. При наличии несохраненных изменений программа предложит сохранить файл.
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Меню Правка -->
			<Border Background="LightGray" Padding="10" Margin="0,5,0,10" CornerRadius="5">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="Bold" Margin="0,0,0,10">
                        Меню "Правка"
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Отменить</Bold> (Ctrl+Z) - отменяет последнее действие.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Повторить</Bold> (Ctrl+Y) - повторяет отмененное действие.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Вырезать</Bold> (Ctrl+X) - вырезает выделенный текст в буфер обмена.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Копировать</Bold> (Ctrl+C) - копирует выделенный текст в буфер обмена.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Вставить</Bold> (Ctrl+V) - вставляет текст из буфера обмена.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Удалить</Bold> (Del) - удаляет выделенный текст.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Выделить все</Bold> (Ctrl+A) - выделяет весь текст в редакторе.
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Меню Текст -->
			<Border Background="LightGray" Padding="10" Margin="0,5,0,10" CornerRadius="5">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="Bold" Margin="0,0,0,10">
                        Меню "Текст"
                    </TextBlock>

					<TextBlock TextWrapping="Wrap" Margin="20,0,0,5" Foreground="Gray">
                        <Italic>Меню "Текст" в разработке.</Italic>
					</TextBlock>
				</StackPanel>
            </Border>

            <!-- Панель инструментов -->
			<Border Background="LightGray" Padding="10" Margin="0,5,0,10" CornerRadius="5">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="Bold" Margin="0,0,0,10">
                        Панель инструментов
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Создать</Bold> (иконка "Лист") - создает новый файл.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Открыть</Bold> (иконка "Папка") - открывает существующий файл.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Сохранить</Bold> (иконка "Дискета") - сохраняет текущий файл.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Назад</Bold> (стрелка влево) - отменяет последнее действие.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Вперед</Bold> (стрелка вправо) - повторяет отмененное действие.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Копировать</Bold> (иконка "Два листа") - копирует выделенный текст.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Вырезать</Bold> (иконка "Ножницы") - вырезает выделенный текст.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Вставить</Bold> (иконка "Буфер обмена") - вставляет текст из буфера.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Вызов справки</Bold> (иконка "Вопрос") - открывает данное окно справки.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5" Foreground="Gray">
                        <Italic>Пуск (иконка "Play") и О программе (иконка "Восклицательный знак") - в разработке</Italic>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Основная область -->
			<Border Background="LightGray" Padding="10" Margin="0,5,0,10" CornerRadius="5">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="Bold" Margin="0,0,0,10">
                        Основная область
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Файл</Bold> - область редактирования текста (RichTextBox). Поддерживает форматирование текста, работает с файлами Poo и TXT.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Вывод</Bold> - область для отображения результатов (DataGrid). В данный момент не реализовано.
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Статусная информация -->
			<Border Background="LightGray" Padding="10" Margin="0,5,0,10" CornerRadius="5">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="Bold" Margin="0,0,0,10">
                        Дополнительная информация
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Индикация изменений:</Bold> В заголовке окна звездочка (*) означает наличие несохраненных изменений в текущем файле.
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="20,0,0,5">
                        <Bold>Форматы файлов:</Bold> Программа поддерживает работу с файлами в форматах Poo и TXT (только текст).
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Версия -->
            <TextBlock 
                FontSize="12" 
                Foreground="Gray" 
                TextAlignment="Center" 
                Margin="0,20,0,0"
                TextWrapping="Wrap">
                Версия программы: 1.0 | Дата последнего обновления: 2026
            </TextBlock>

        </StackPanel>
    </ScrollViewer>
</Window>